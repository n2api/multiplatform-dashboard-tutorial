import logo from './logo.svg';
import './App.css';

import Dashboard from './Dashboard'

/**
 * The main component of the dashboard.
 *
 * This component renders the campaign dashboard.
 */

const App = () => {
  /**
   * The main component of the application.
   *
   * This component renders the header, logo, and a link to the React documentation.
   */
  return (
    <div>
      <Dashboard />
    </div>
  )
}

export default App;
